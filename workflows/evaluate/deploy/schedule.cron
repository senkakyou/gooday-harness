# runas: root
# 每天 08:00 跑一遍所有评价器。
# 评价器只管怎么打分，什么时候跑由本 workflow 统一负责——
# 否则每加一个评价器就要加一条 cron，迟早有人忘（policies G17）。
0 8 * * * bash -c 'set -a; source /opt/gooday-harness/.env 2>/dev/null; set +a; python3 /opt/gooday-harness/workflows/{{NAME}}/run.py' >> /var/log/gooday-harness/{{NAME}}.log 2>&1
