# 每小时热备一份，滚动 24 份（按小时命名自动覆盖）。
# 与旧 db-snapshot（每小时 :07）错开，避免两者同时读同一个库。
# 03 点档归档日快照保留 7 份；04 点档做恢复演练。
22 * * * * bash -c 'set -a; source /opt/gooday-harness/.env 2>/dev/null; set +a; python3 /opt/gooday-harness/workflows/{{NAME}}/run.py' >> /var/log/gooday-harness/{{NAME}}.log 2>&1
