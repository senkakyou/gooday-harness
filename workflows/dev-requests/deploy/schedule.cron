# runas: root
# 处理开发需求工单。频率按日志时间戳反推（root crontab 读不到），观察到 :05 与 :45 各一次。
5,25,45 * * * * bash -c 'set -a; source /opt/gooday-harness/.env 2>/dev/null; set +a; python3 /opt/gooday-harness/workflows/{{NAME}}/run.py' >> /var/log/gooday-harness/{{NAME}}.log 2>&1
