# runas: agent
# 奇妙数学动画产线，每日 02:00 出 20 集。以 agent 跑——要用它的 claude 凭据。
0 2 * * * bash -c 'set -a; source /opt/gooday-harness/.env 2>/dev/null; set +a; flock -n /tmp/math-next.lock python3 /opt/gooday-harness/workflows/{{NAME}}/run.py 20' >> /var/log/gooday-harness/{{NAME}}.log 2>&1
