# runas: agent
# 成语时光机：每 3 天推一次喜马拉雅上传清单。
0 21 */3 * * bash -c 'set -a; source /opt/gooday-harness/.env 2>/dev/null; set +a; python3 /opt/gooday-harness/workflows/{{NAME}}/run.py' >> /var/log/gooday-harness/{{NAME}}.log 2>&1
