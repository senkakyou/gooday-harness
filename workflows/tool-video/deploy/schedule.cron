# runas: root
# 讲解片产线。以 root 跑是因为它要读运行库（docker 卷，agent 读不到）；
# 而调模型要的凭据靠 HOME=/home/agent 指过去——【凭据是同一个物理文件，
# 绝不复制第二份】：OAuth 的 refreshToken 会轮换，两份副本各自刷新会互相顶掉
# （旧系统栽过两次，其中一次 6 个 bot 连续多天"活着但答不出话"）。
#
# 每周一 03:20 出一条：挑「已发布 + 还没有讲解 + 有人用过」里下载量最高的那个。
# 一次只做一个是刻意的——出错影响面只有一个工具，且人有时间看完再决定要不要继续。
20 3 * * 1 bash -c 'set -a; source /opt/gooday-harness/.env 2>/dev/null; set +a; HOME=/home/agent python3 /opt/gooday-harness/workflows/{{NAME}}/run.py' >> /var/log/gooday-harness/{{NAME}}.log 2>&1

# 每周日 04:00 自检：不调模型渲一条样片。
# 【为什么单独有这一条】：主任务每周只跑一次，渲染链路（chromium / edge-tts / ffmpeg）
# 哪天坏了，不自检的话要等到下周一才知道，而那时看到的错会被当成"这个工具不好写"。
0 4 * * 0 bash -c 'set -a; source /opt/gooday-harness/.env 2>/dev/null; set +a; HOME=/home/agent python3 /opt/gooday-harness/workflows/{{NAME}}/run.py --self-test' >> /var/log/gooday-harness/{{NAME}}.log 2>&1
