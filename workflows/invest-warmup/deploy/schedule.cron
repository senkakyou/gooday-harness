# runas: agent
# 投资自选预热，工作日 08:05。接口重建 140 只要 150~190 秒，直连 8081 绕开 nginx 超时。
5 8 * * 1-5 bash -c 'set -a; source /opt/gooday-harness/.env 2>/dev/null; set +a; bash /opt/gooday-harness/workflows/{{NAME}}/run.sh' >> /var/log/gooday-harness/{{NAME}}.log 2>&1
