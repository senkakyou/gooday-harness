# 本服务要调模型，需要 Claude 凭据。
#
# 凭据是全线单点，且【绝不能复制成多份】：OAuth 的 refreshToken 会轮换，
# 两份副本各自刷新会互相顶掉（旧系统真栽过两次，其中一次 6 个 bot
# 连续多天「活着但答不出话」）。所以让进程指向【同一个物理文件】。
#
# drop-in 跟着需要它的服务走，不集中放 ops/——新增服务不必改公共目录（G06）。
[Service]
Environment=HOME=/home/agent
