// =====================================================
// appsettings.Development.json —— 本地开发环境专用配置
// 当 ASPNETCORE_ENVIRONMENT=Development 时，此文件的值会覆盖 appsettings.json
// =====================================================
{
  // 开发环境数据库：存在项目根目录，方便直接用 sqlite3 命令查看
  "ConnectionStrings": {
    "Default": "Data Source=gooday.db"
  }
}
