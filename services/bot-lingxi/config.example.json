{
  "_readme": "拷贝为 config.json。",
  "mode": "active",
  "bot_id": 20,
  "serves": [
    1
  ],
  "_serves": "【只服务站长】。这不是礼貌问题——是「谁能驱动一个有 shell 的角色」的问题。",
  "may_send_to": [
    1
  ],
  "_may_send_to": "只能发站长。它不接触客户、也不直接指挥其他角色。默认拒绝。",
  "allowed_tools": [
    "Bash",
    "Read",
    "Write",
    "Edit",
    "Glob",
    "Grep"
  ],
  "_allowed_tools": "工具权限走配置不写死在代码里：不同环境该给的权限不同，而且改权限应该是一次可 review 的配置变更。收紧时把不需要的删掉即可。注意 WebFetch/WebSearch 已默认移除——它们会把外部不可信内容直接引入一个有 Bash 的会话。",
  "db": "/var/lib/docker/volumes/gooday_gooday_data/_data/gooday.db",
  "api_base": "https://localhost",
  "model": "opus",
  "poll_sec": 3,
  "timeout_sec": 600,
  "username": "灵犀",
  "role": "admin",
  "_username": "签 JWT 用。必须与库里 Users.Username 一致。",
  "_mode": "",
  "extra_dirs": [
    "/srv/gooday-harness/work",
    "/srv/gooday-harness/media",
    "/srv/gooday-harness/backups",
    "/var/lib/gooday-harness",
    "/var/log/gooday-harness",
    "/tmp/blind-a1"
  ],
  "_extra_dirs": "大海 2026-09-11：「给灵犀加满权限，她代表着我」。这里加满的是【本系统自己的目录】：交付物、产物、备份、证据链、日志。她的进程以 root 跑（unit 里没有 User=），所以这个沙箱白名单是唯一的实际控制——写进来的目录她就真能读写。【刻意没加两样，见 docs/decisions/006 与 known-gaps 缺口十九】：① 运行库所在的 docker 卷目录——给了等于给她绕过所有 API 闸门直接改库的能力；② WebFetch/WebSearch 不在 allowed_tools 里——它们会把外部不可信内容引入一个有 Bash 的会话，这条是注入防线不是权限吝啬。这两样要加，由大海单独拍。"
}
