# NNN · 一句话说清决定了什么

> 日期：YYYY-MM-DD · 决策人： · 状态：提议 / 已批准 / 实验中 / 已上线 / 已否决
> 文件名：`NNN-短横线标题.md`，NNN 三位递增，**不复用编号**（编号是引用锚点）

> **G07 要求：任何自动改动都必须有一份 Decision，且进版本库。**
> 运行期数据（Task/Event/Evidence/Evaluation/Checkpoint）可以过期清理，
> 决策记录不能——它是唯一能回答「三个月后为什么当初这么改」的东西。

## 触发

什么让我们要改？填一条：

- [ ] 某个 Evaluation 判了 `fail` → 引用它：`/var/lib/gooday-harness/evaluations/xxx.json`
- [ ] 某次事故 → 引用复盘：`docs/incidents/xxx.md`
- [ ] 外部变化（依赖下线、平台策略变更）
- [ ] 主动改进（**这一档要格外警惕**：没有证据支撑的「优化」多半是自我感动）

## 现状

现在是怎么做的、有什么问题。**要有可观测的量**，不能只说「不太好」。

## 决定

改成什么。具体到文件和行为，不要停在方向上。

## 备选方案与放弃理由

**必填。** 只有一个方案说明没想清楚。写下否决了什么、为什么否决——
三个月后有人想「怎么不用 X 呢」，答案应该在这里，而不是重新讨论一遍。

## 风险与代价

诚实写。包括**这个改动会让什么变差**——凡是只有好处的改动，都是没想全。

## 验证方式

关联实验：`evolution/experiments/<名字>/`

- 通过判据（**必须先写死**，不许跑完再定）：
- 回滚点：`/var/lib/gooday-harness/checkpoints/<名字>/`
- 回滚命令：

## 结论

> 实验跑完后回填。**没有结论的 Decision 等于这次改动没发生过。**

- 实验结果：
- 判定：通过 / 否决
- 上线时间 / 回滚时间：
