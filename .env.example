# Gooday Harness 密钥声明
#
# 【本文件进版本库，作用是声明需要哪些密钥】——新机器部署时照着填。
# 它本身【不含任何真值】，每个值都必须是占位符（policies G18 会检查）。
#
# 真文件：/opt/gooday-harness/.env，权限 0600，【绝不进版本库】。
#
# 设计见 docs/specs/004-secrets-and-config.md：
# 密钥和配置是分开的——配置进版本库（可 review、可回滚、是真源），
# 密钥不进。混在一起的话，改一个模型名也要碰密钥文件，
# 而且「当前生效的配置是什么」会失去单一真源。

# ── 真密钥 ──────────────────────────────────────────────
# API 鉴权签名密钥。换它会让所有已签发的 token 立即失效——
# 轮换流程尚未设计，见 spec 004「尚未决定」。
JWT_SECRET=replace_with_strong_random_secret

# ── 大模型 ──────────────────────────────────────────────
# 留空则走 OAuth 凭据文件（/home/agent/.claude/.credentials.json）。
# ⚠️ 那个文件是全线单点，且【绝不能复制成多份】：
#    OAuth 的 refreshToken 会轮换，两份副本各自刷新会互相顶掉。
#    旧系统在这上面栽过两次，其中一次 6 个 bot 连续多天「活着但答不出话」。
# 长期根治是改用 API key——不过期、不刷新、无轮换竞争、额度隔离。
ANTHROPIC_API_KEY=

# ── 注意 ────────────────────────────────────────────────
# 普通配置【不要往这里加】。模型名、开关、阈值这些属于配置，
# 放各模块自己的 config.json 或全局 ops/config.example.json。
# 这个文件只放「泄露了会出事」的东西。
